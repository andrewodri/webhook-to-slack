{
  "name": "webhook-to-slack",
  "version": "1.0.0",
  "description": "Translate webhook payloads into Slack RTM/Web API integrations",
  "main": "app.js",
  "scripts": {
    "test": "tape tests/**/*.js",
    "tag": "git fetch --tags && git tag -a $npm_config_tag -m $npm_config_tag $(git rev-parse HEAD) -f && git push origin --tags -f"
  },
  "author": "Andrew Odri",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/andrewodri/webhook-to-slack.git"
  },
  "dependencies": {
    "body-parser": "^1.16.0",
    "express": "^4.14.1",
    "minimist": "^1.2.0",
    "moment": "^2.17.1",
    "slack": "^8.2.0"
  },
  "devDependencies": {
    "tape": "^4.6.3"
  }
}
